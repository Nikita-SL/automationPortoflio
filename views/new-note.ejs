<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>New Note</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container py-5">
  <h2>New Note</h2>
  <% if (error) { %>
    <div class="alert alert-danger" data-cy="error"><%= error %></div>
  <% } %>
  <form method="POST" action="/notes/new">
    <div class="mb-3">
      <label for="content" class="form-label">Note</label>
      <textarea data-cy="note-content" class="form-control" id="note-content" name="content" required></textarea>
    </div>
    <button data-cy="save-note" type="submit" class="btn btn-primary">Save Note</button>
    <a href="/dashboard" class="btn btn-link">Back</a>
  </form>
</body>
</html> 